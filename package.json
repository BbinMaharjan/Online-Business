{
  "name": "online-business",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix admin\"",
    "dev:server": "npm run dev --prefix server",
    "dev:admin": "npm run dev --prefix admin",
    "build": "npm run build --prefix server && npm run build --prefix admin",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}